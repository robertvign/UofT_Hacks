## Packages
framer-motion | Smooth animations for "juicy" UI interactions (hover, tap, progress bars)
clsx | Utility for conditional class names
tailwind-merge | Utility for merging tailwind classes

## Notes
Integration: "STRICTLY CLIENT-SIDE ONLY" mode active.
API Hooks will simulate network delays and return mock data instead of real fetch calls.
Layout uses a mobile-first responsive design with a bottom nav on small screens and header on large.
